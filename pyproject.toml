[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "biom3"
dynamic = ["version"]
requires-python = ">= 3.8"
readme = "README.md"
description = "BioM3 development and investigation."
authors = [
  {
    name = "Addison Howe", 
    name = "Tarak Nandi",
  }
]

[project.scripts]
biom3 = "Stage3_source.__main__:main"
biom3-test = "Stage3_source.__main__:run_test"
