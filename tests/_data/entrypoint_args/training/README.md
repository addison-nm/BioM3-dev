# Training script entrypoint arguments

#### `training_args_v1.txt`

Loads minimodel1 weights into minimodel1 before training on swissprot data.

#### `training_args_v2.txt`

Attempts to load minimodel2 model weights into minimodel1 architecture, so we expect an error.

